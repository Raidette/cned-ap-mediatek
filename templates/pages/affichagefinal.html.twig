{% extends "basefront.html.twig" %}


{% block body %}

    {{ dump(formation) }}
    {{ dump(titre) }}
    {{ dump(description) }}
    {{ dump(categories) }}
    {{ dump(playlist) }}
    {{ dump(datePublished) }}

{% endblock %}